export default function Home() {
  return (
    <div style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>Auditi Report AI Endpoint</h1>
      <p>Send a POST request to <code>/api/audit</code> with a multipart/form-data payload.</p>
    </div>
  );
}
